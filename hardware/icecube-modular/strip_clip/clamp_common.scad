strip_width = 12.5;
strip_thickness = 4;

pad_width = 8;
length = 10;

base_width = strip_width + 2*pad_width;
base_length = length;
base_thickness = 3;
base_size = [base_width, base_length, base_thickness];

plate_thickness = 3;
plate_hole_radius = 1.5;

pin_radius_base = 1.5;
